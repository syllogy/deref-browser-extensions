overwrite: true
schema: "graphql/schema.json"
documents: "graphql/**/*.graphql"
generates:
  ./graphql/types.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
