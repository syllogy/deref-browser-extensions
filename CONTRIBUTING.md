# Contributing to the Deref Browser Extensions

## Building

1. Pull the repository
   `bash git clone git@github.com:deref/wext.git && cd wext `

2. Install the dependencies
   `npm install`

3. Start the dev environment
   `npm run build`
