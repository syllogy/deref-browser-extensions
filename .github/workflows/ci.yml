---
name: ci
on: [push, pull_request]
jobs:
  build_extension:
    name: "Build extension"
    runs-on: ubuntu-latest
    container:
      image: "node:lts"
    steps:
      - name: Install dependencies
        run: 'npm install'
      - name: Compile
        run: 'npm run compile'
      - name: Lint
        run: 'npm run lint'

